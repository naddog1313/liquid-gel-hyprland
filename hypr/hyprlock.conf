source = ~/.config/omarchy/current/theme/hyprlock.conf

general {
    ignore_empty_input = true
}

background {
    monitor =
    color = $color
    path = /home/dog/.config/omarchy/themes/mac/backgrounds/mactahoe.png
    blur_passes = 3
}
image {
    monitor =
    path = ~/Downloads/finnman.png
    size = 150
    rounding = -1 # Makes it a circle
    border_size = 4
    border_color = rgba(255, 255, 255, 0.5)
    position = 0, 80
    halign = center
    valign = center
}

animations {
    enabled = false
}

input-field {
    monitor =
    size = 200, 50
    outline_thickness = 3
    dots_size = 0.33
    dots_spacing = 0.15
    dots_center = true
    outer_color = rgba(255, 255, 255, 0) # Transparent border
    inner_color = rgba(255, 255, 255, 0.1) # Glass effect
    font_color = rgb(200, 200, 200)
    fade_on_empty = true
    placeholder_text = <i>Enter Password</i>
    position = 0, -20
    halign = center
    valign = center
}
label {
    monitor =
    text = $TIME
    color = rgba(255, 255, 255, 1.0)
    font_size = 120
    font_family = SF-mono
    position = 0, -100
    halign = center
    valign = top
}

auth {
    fingerprint:enabled = false
}
